{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project",
            "type": "shell",
            "command": "gcc",
            "args": [
                "${workspaceFolder}\\main.c",
                "${workspaceFolder}\\prescription.c",
                "-o",
                "${workspaceFolder}\\pharmacy.exe"
            ],
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"]
        }
    ]
}